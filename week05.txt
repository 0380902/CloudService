1교시
git clone 원격리포지토리주소 (이건 파일안에 CloudService 파일이 하나 또 생기는거)
git clone 원격리포지토리주소 . (이건 파일안에 내용만 그대로 있는거)
git config --list

리눅스 명령어
vi 편집기 화면이 나타나면 j(아래), k(위), q(빠져나오기)

2교시
다른 사람의 원격리포지토리를 클론 후 새파일 추가 또는 기존파일 편집 후 로컬리포지토리에 커밋

원 저작자와 바인딩된 원격리포지토리 주소를 내 원격리포지토리 주소를 내 원격리포지토리 주소로 업데이트

확인 git remote -v
삭제 git remote remove origin
등록 git remote add origin 내가만든 원격리포지토리주소
확인 git remote -v

업로드 git push
 ㄴ 오류 발생? (새로 만든 깃 리모트 리포지토리 주소와 바인딩 필요)
업로드 git push -u origin main